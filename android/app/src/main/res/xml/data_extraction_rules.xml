<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+: nothing of DAWASA's private data (database, receipt photos,
     preferences, secure storage) is included in cloud backups or
     device-to-device transfers. Users move their data with DAWASA's own
     password-encrypted backup file instead. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
